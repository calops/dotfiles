local palette = {}

palette.colors = {
    base = '',
    overlays = { '', '', '', },
    highlights = { '', '', '', },
    accents = { '', '', '', },
    diagnostics = {},
}

function palette.darken(color, bg, blend)
end

function palette.brighten(color, bg, blend)
end

return palette
